{
  "name": "auth-service-with-notifications",
  "version": "1.0.0",
  "private": "true",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "prisma-studio": "docker compose exec app npx prisma studio --port 5555",
    "prisma-migrate-dev": "docker compose exec app npx prisma migrate dev"
  },
  "devDependencies": {}
}
