{
  "name": "auth-service-with-notifications",
  "version": "1.0.0",
  "private": "true",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {}
}
